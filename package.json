{
  "name": "umbrella-couture-app",
  "version": "1.0.0",
  "description":
    "client + server for controlling leds in umbrella, necklace, and skirt using MQTT",
  "main": "index.js",
  "scripts": {
    "log": "pm2 logs index",
    "monitor": "pm2 monitor",
    "show": "pm2 show index",
    "start": "PORT=8080 pm2 start index.js",
    "restart": "pm2 restart all",
    "install": "cd client && npm install && npm run build && cd .."
  },
  "author": "Stephanie Nemeth",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^4.0.0",
    "express": "^4.16.2",
    "mosca": "^2.7.0",
    "pm2": "^2.9.1"
  }
}
